# SnapSum Shared (Planned)

Reserved for shared TypeScript packages:

- Domain types (`Transaction`, `Budget`, `Category`)
- Validation schemas (Zod)
- Shared utility logic (money formatting, date handling)

Create this as a package workspace once the backend repo is initialized.
