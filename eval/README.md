# SnapSum Parsing Evaluation (Planned)

Reserved for receipt parsing evaluation harness:

- Private receipt corpus metadata
- Field-level accuracy scripts (`merchant`, `amount`, `date`, `category`)
- Latency benchmarks by device tier

Start with JSON fixtures and a small CLI runner before adding model-specific benchmarks.
